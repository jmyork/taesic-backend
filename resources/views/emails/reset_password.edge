<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recuperar palavra-passe — Alaragest</title>
    @include('emails/partials/alaragest_styles')
  </head>
  <body>
    <div class="wrapper">
      <div class="card">
        @include('emails/partials/alaragest_header')

        <div class="body">
          <p class="eyebrow">Segurança</p>
          <h1>Recuperar palavra-passe</h1>
          <p class="subtitle">Olá, {{ user.username }}</p>

          <p>
            Recebemos um pedido para redefinir a palavra-passe da conta
            <span class="highlight">{{ user.email }}</span>.
          </p>

          <div class="cta-wrap">
            <a href="{{ resetUrl }}" class="cta-btn">Redefinir palavra-passe</a>
          </div>

          <div class="fallback">
            <p class="fallback-label">Link alternativo</p>
            <p class="fallback-url">{{ resetUrl }}</p>
          </div>

          <p style="font-size: 13px; color: #6b7280;">
            Se não fez este pedido, ignore este email. A sua palavra-passe permanece inalterada.
          </p>
        </div>

        <div class="footer">
          <p>© {{ new Date().getFullYear() }} Alaragest — Todos os direitos reservados.</p>
        </div>
      </div>
    </div>
  </body>
</html>
