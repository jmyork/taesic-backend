{{-- Registo de uma empresa: falta confirmar o email do dono para a conta ficar activa. --}}
@component('emails/components/carta', {
  year,
  preheader: 'Um clique confirma o seu email e activa a conta. Válido 24 horas.'
})

  @component('emails/components/titulo')
    Confirme o seu email, {{ user.firstName }}
  @end

  @component('emails/components/texto')
    O registo da empresa <span class="strong" style="font-weight:600;color:#0f172a;">{{ user.companyName }}</span>
    foi criado com o email <span class="strong" style="font-weight:600;color:#0f172a;">{{ user.email }}</span>.
    Falta confirmar este endereço — depois entra com a palavra-passe que escolheu no registo.
  @end

  @!component('emails/components/botao', {
    url: verifyUrl,
    texto: 'Activar a conta',
    nota: 'Válido 24 horas, uma única vez — se expirar, a página pede um email novo.'
  })

  @component('emails/components/nota')
    Se não foi você a criar esta conta, ignore este email: sem confirmação a conta não é activada.
  @end

@end
