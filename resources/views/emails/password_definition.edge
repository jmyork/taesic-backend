<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Conta criada — Alaragest</title>
    @include('emails/partials/alaragest_styles')
    <style>
      .credentials-box {
        background: #f9fafb;
        border: 1px solid #e5e7eb;
        border-radius: 8px;
        padding: 20px;
        margin: 24px 0;
        font-size: 14px;
      }
      .credentials-label {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #9ca3af;
        margin-bottom: 4px;
      }
      .credentials-value {
        font-family: ui-monospace, monospace;
        font-weight: 600;
        color: #172554;
        margin-bottom: 14px;
        word-break: break-all;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="card">
        @include('emails/partials/alaragest_header')

        <div class="body">
          <p class="eyebrow">Nova conta</p>
          <h1>Bem-vindo, {{ user.username }}</h1>
          <p>
            A sua conta em <span class="highlight">{{ company.name }}</span> foi criada.
            Utilize os dados abaixo no primeiro acesso e altere a palavra-passe em seguida.
          </p>

          <div class="credentials-box">
            <p class="credentials-label">Email</p>
            <p class="credentials-value">{{ user.email }}</p>
            <p class="credentials-label">Utilizador</p>
            <p class="credentials-value">{{ user.username }}</p>
            <p class="credentials-label">Palavra-passe temporária</p>
            <p class="credentials-value">{{ temporaryPassword }}</p>
          </div>

          <div class="cta-wrap">
            <a href="{{ resetUrl }}" class="cta-btn">Aceder à plataforma</a>
          </div>

          <div class="signoff">
            <p>Equipa Alaragest</p>
          </div>
        </div>

        <div class="footer">
          <p>© {{ new Date().getFullYear() }} Alaragest — Todos os direitos reservados.</p>
        </div>
      </div>
    </div>
  </body>
</html>
