{{-- Conta de funcionário criada por um administrador. NÃO leva palavra-passe: a temporária
     gerada no servidor nunca é usada, o acesso é sempre pelo link. --}}
@component('emails/components/carta', {
  year,
  preheader: 'A sua conta foi criada. Falta definir a palavra-passe — escolhe-a você.'
})

  @component('emails/components/titulo')
    Bem-vindo, {{ user.username }}
  @end

  @component('emails/components/texto')
    A sua conta em <span class="strong" style="font-weight:600;color:#0f172a;">{{ company.name }}</span>
    foi criada com o email <span class="strong" style="font-weight:600;color:#0f172a;">{{ user.email }}</span>.
    Falta só definir a palavra-passe — escolhe-a você, ninguém mais lhe tem acesso.
  @end

  @!component('emails/components/botao', {
    url: resetUrl,
    texto: 'Definir a minha palavra-passe',
    nota: 'Válido 24 horas, uma única vez.'
  })

  @component('emails/components/nota')
    Nunca enviamos palavras-passe por email. Se receber uma mensagem em nome do TAESIC com
    uma palavra-passe escrita, desconfie e não a use.
  @end

@end
