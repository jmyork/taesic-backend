<!DOCTYPE html>
<html lang="pt">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recuperação de palavra-passe — Alaragest</title>
    @include('emails/partials/alaragest_styles')
  </head>
  <body>
    <div class="wrapper">
      <div class="card">
        @include('emails/partials/alaragest_header')

        <div class="body">
          <p class="eyebrow">Segurança</p>
          <h1>Redefinir palavra-passe</h1>
          <p>Olá, <span class="highlight">{{ user.username }}</span>,</p>

          <p>
            Recebemos um pedido para redefinir a palavra-passe da conta
            <span class="highlight">{{ user.email }}</span>.
          </p>

          <div class="cta-wrap">
            <a href="{{ resetUrl }}" class="cta-btn">Redefinir palavra-passe</a>
          </div>

          @if(expiresIn)
            <div class="notice">
              <p><strong>Atenção:</strong> este link é válido por <strong>{{ expiresIn }}</strong>.</p>
            </div>
          @endif

          <div class="signoff">
            <p>Equipa Alaragest</p>
          </div>
        </div>

        <div class="footer">
          <p>© {{ new Date().getFullYear() }} Alaragest — Todos os direitos reservados.</p>
        </div>
      </div>
    </div>
  </body>
</html>
