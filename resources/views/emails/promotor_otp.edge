{{-- Código de acesso ao painel de promotor. O código é texto (não imagem): continua
     seleccionável e copiável em qualquer cliente. --}}
@component('emails/components/carta', {
  year,
  preheader: 'O seu código de acesso ao painel de promotor. Válido 10 minutos.'
})

  @component('emails/components/titulo')
    O seu código de acesso
  @end

  @component('emails/components/texto')
    Olá, {{ nome }}. Introduza este código no ecrã de entrada para abrir o seu painel:
  @end

  @component('emails/components/painel', { alinhamento: 'center' })
    <div class="code panel-value" style="font-family:'Segoe UI',Consolas,Menlo,Monaco,monospace;font-size:34px;line-height:42px;font-weight:700;letter-spacing:10px;color:#172554;">{{ codigo }}</div>
  @end

  @component('emails/components/aviso')
    Válido durante <strong style="font-weight:700;">10 minutos</strong> e só pode ser usado
    uma vez. Pedir um código novo anula este.
  @end

  @component('emails/components/nota')
    Se não foi você a pedir este código, ignore este email — e nunca o partilhe com ninguém.
  @end

@end
